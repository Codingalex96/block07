const msg = "You have received this message because you have been chosen to open an important vault. Here is the secret combination: ";

// combo 1 is 10 so i did 20-10 for 10 //
const num1 = 20 - 10;


const num2 = 20 * 2;

// here i had the alert show the message then the corresponding numbers with dashes in between to creat spaces
const num3 = 40 - 1;
alert(`${msg} ${num1} - ${num2} - ${num3}`);